#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int i= -5;
    double x=5.0;
    double y= 2.0;
    double z=5.21;

    cout<<"\n the absolute value of i is: "<< abs(i);
    cout<<"\n the power of x to y: "<< pow(x,y);
}